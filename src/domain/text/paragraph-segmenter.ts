export interface TextRange { start: number; end: number; }
export function segmentParagraphs(text: string): TextRange[] { const result: TextRange[] = []; const boundary = /\n[\t ]*\n+/g; let start = 0; let match: RegExpExecArray | null; while ((match = boundary.exec(text))) { if (match.index > start) result.push({ start, end: match.index }); start = match.index + match[0].length; } if (start < text.length) result.push({ start, end: text.length }); return result; }
